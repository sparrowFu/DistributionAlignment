# MCDisp-Align 消融实验方案

依据：[main.md](./main.md) 中最终的 Introduction 和第三章 Method。方案仅消融三个关键约束，共设置四组实验。

| 实验组 | 修改内容 | 验证目的 |
|---|---|---|
| **Full MCDisp-Align** | 保留全部模块 | 作为比较基准 |
| **w/o KL Alignment** | 去掉 KL 对齐损失 | 验证 caption-set 的 semantic center 和 semantic range 对图像分布的直接指导是否有效 |
| **w/o Caption Coverage** | 去掉逐条 caption 覆盖损失 | 验证覆盖每条 caption 是否有助于保留不同描述表达的语义 |
| **w/o Direction Alignment** | 去掉方向对齐损失 | 验证 caption-set 的 main variation directions 是否为图像分布提供有效指导 |

四组均保留相同的概率表示、caption-set distribution construction 和双向匹配。每组使用相同的图像及其全部关联 captions，保留相同的弱方差先验。

消融时只将对应损失的权重设为零，其余损失权重、模型结构和训练设置保持一致。**去掉方向对齐时仍保留低秩分支**，以保证比较反映方向监督的作用。

在 MSCOCO 和 Flickr30K 上分别报告 image-to-text 与 text-to-image 的 Recall@1、Recall@5、Recall@10。主表可精简展示 I→T R@1、T→I R@1 和 mR，其中 mR 为双向六个 Recall 指标的平均值。

每个消融组直接与完整模型比较。所有组使用相同的数据划分、候选库、验证选模规则和单图像、单 caption 独立推理协议。

结果表分别为两个数据集填写，实验完成前保留空位：

| 实验组 | I→T R@1 | T→I R@1 | mR |
|---|---|---|---|
| Full MCDisp-Align | 待填 | 待填 | 待填 |
| w/o KL Alignment | 待填 | 待填 | 待填 |
| w/o Caption Coverage | 待填 | 待填 | 待填 |
| w/o Direction Alignment | 待填 | 待填 | 待填 |
